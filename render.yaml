services:
  - type: web
    name: bmi-mcp-server
    runtime: node
    plan: free
    region: oregon
    buildCommand: "npm install"
    startCommand: "node server.mjs"
    envVars:
      - key: NODE_ENV
        value: production
